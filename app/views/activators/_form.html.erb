<%= form_for(activator) do |f| %>
  <% if activator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activator.errors.count, "error") %> prohibited this activator from being saved:</h2>

      <ul>
      <% activator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :commandline %>
    <%= f.text_field :commandline %>
  </div>

  <div class="field">
    <%= f.label :color %>
    <%= f.text_field :color %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
